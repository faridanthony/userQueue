## ✅ Cohort diff-review — `approve`

_Small, clean addition of a read-only inspection method. The implementation is correct and the docblock accurately describes the behaviour._

- **NIT** `class.Queue.inc:23` — class.Queue.inc:23 – The docblock says 'shallow copy', which is true, but worth noting that if `_queue` contains objects those references are shared. Consider mentioning 'object references are not cloned' if callers could mutate contained objects.

_Reviewed by claude-sonnet-4-6 via Cohort._