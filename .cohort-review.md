## 💬 Cohort diff-review — `comment`

_The empty-queue guard is a good defensive addition, but `array_pop` removes from the *end* of the array (LIFO), while the class is named `userQueue` implying FIFO. The docblock says 'front item' which contradicts `array_pop` semantics. This may be a pre-existing bug, but the new docblock now formally documents incorrect behavior._

- **MINOR** `class.Queue.inc:17` — class.Queue.inc:17 – `array_pop` removes the *last* element (LIFO/stack semantics), but the docblock on line 9 says 'Remove and return the front item of the queue' (FIFO semantics). If this is meant to be a queue, `array_shift` should be used instead (or the docblock corrected to say 'last item'). This inconsistency was introduced/formalized by the new docblock in this PR.

_Reviewed by claude-sonnet-4-6 via Cohort._